"""Bike rental domain core (hexagonal architecture).

This package contains the pure domain (entities, value objects, ports and use
cases) plus in-memory adapters. The domain MUST NOT import any framework or
perform real I/O (ADR-0008).
"""
