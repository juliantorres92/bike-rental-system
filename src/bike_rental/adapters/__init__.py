"""In-memory adapters implementing the rental ports (ADR-0008).

These are the driven-side adapters used for testing the domain in isolation —
no FastAPI, no SQLAlchemy, no PostgreSQL, no real I/O or network.
"""
